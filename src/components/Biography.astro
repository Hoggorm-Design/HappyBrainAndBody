---
const {
    title,
    buttonText,
    buttonLink,
    imageSrc,
    imageAlt,
    name,
    degree,
    info,
} = Astro.props;
---

<div class="container">
    <h2>{title}</h2>
    <div class="gray-background-container">
        <div class="image-container">
            <img src={imageSrc} alt={imageAlt} />
        </div>
        <div class="text-container">
            <h4>{name}</h4>
            <p style="margin-top:-20px;">{degree}</p>
            <p>{info}</p>
        </div>
    </div>
    <a href="https://eqterapeuter.no/eq-terapeuter-oslo/" target="_blank">
        <button>{buttonText}</button>
    </a>
</div>

<style>
    .container {
        margin-top: 200px;
    }

    .gray-background-container {
        background-color: #e8e8e8;
        display: flex;
        padding: 30px;
        gap: 50px;
        margin-top: 50px;
    }

    button {
        margin-top: 50px;
        background-color: white;
        border: 1px solid black;
        color: black;
        padding: 15px 25px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 12pt;
    }

    button:hover {
        cursor: pointer;
        background-color: #e8e8e8;
    }
</style>
